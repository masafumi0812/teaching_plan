import 'package:flutter/material.dart';

class TrainingPlanKeyFactorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("キーファクター")),
    );
  }
}
