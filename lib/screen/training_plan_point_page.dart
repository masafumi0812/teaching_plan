import 'package:flutter/material.dart';

class TrainingPlanPointPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ポイント")),
    );
  }
}
