import 'package:flutter/material.dart';

class TrainingPlanBoardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ボード")),
    );
  }
}
