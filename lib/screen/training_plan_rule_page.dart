import 'package:flutter/material.dart';

class TrainingPlanRulePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ルール")),
    );
  }
}
