name: teaching_plan_app
description: Soccer Teaching Plan App

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  provider: ^4.3.3
  flutter_slidable: ^0.5.7
  ff_navigation_bar: ^0.1.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/
    - images/screen_image/